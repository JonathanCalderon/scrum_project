

import * as mocha from 'mocha';

import { expect } from 'chai';
import { assert } from 'chai';

describe('Calculator', () => {
  it('should add two numbers', () => {

    // Chai assertion.
    expect(8).to.equal(8);
  });
});
